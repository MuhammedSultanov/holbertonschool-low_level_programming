#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function
 * @argc: argument count
 *
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
int i = 0;
for (i = 0; i < argc; i++)
{}
while (argv[len] != '\0')
{
len++
}
printf("%d\n", len);
return (0);
}
